# 第三问候选复现入口

本目录仅为 `99_临时中转` 中的第三问候选工作区，尚未通过独立审核前不得复制到 `03_第三问`。

计划的一键入口：

```powershell
.\一键运行第三问多因素建模.ps1
```

复现流程将依次完成数据重建、候选逐孕妇留一验证、主模型拟合、BMI 全组数分段、逐日前沿、技术误差传播、孕妇整簇自助、参数表、自审和哈希。流程不生成图片，只生成 MATLAB/SVG 制图提示词 TXT。
