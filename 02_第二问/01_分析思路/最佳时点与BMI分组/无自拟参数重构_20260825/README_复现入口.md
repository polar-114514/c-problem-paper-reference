# 第二问重构复现入口

运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\一键运行第二问无自拟参数重构.ps1
```

脚本从第一问2026-08-25当前复现包读取613个抽血事件和记录层数据，重建首次达标删失区间、技术复测误差、三种AFT候选、全部可识别组数比较、Pareto前沿、检测误差传播、孕妇整簇自助、质量口径敏感性和自审材料。

本入口不设置可靠性阈值 `q`、风险权重、候选组数上限或最小组人数，不生成图片，不写论文正文，也不修改第二问正式目录。只有 `正式候选输出/04_复现/第二问自审PASS记录.json` 为 `PASS` 时才允许归档。
