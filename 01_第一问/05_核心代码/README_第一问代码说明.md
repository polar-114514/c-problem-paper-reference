# 第一问核心代码说明

本目录中的两个Python文件是当前第一至第三问主脚本的论文参考副本。统计计算逻辑未改动；`补充主审整改.py`只增加了可显式指定AFT冻结证据目录的路径接口，避免整理后依赖失效。

## 对应关系

- `优先问题整改分析.py`：读取男胎记录、构造抽血事件、解析孕周、分解妇间与个体内BMI，并保留第一问历史敏感性分析；
- `补充主审整改.py`：`第一问分段GEE`函数生成当前第一问主模型参数、前后段派生效应和联合稳健Wald检验。

## 论文写作建议

正文只需用伪代码概括“事件构造—变量中心化—分段GEE—稳健Wald检验”。完整Python代码适合放在支撑材料或代码附件，不建议整段贴入正文。

## 复现入口

不要直接使用默认输出目录写入问题文件夹。统一运行：

`05_公共材料/06_核心代码索引/一键复现当前四问.ps1`

该入口显式指定原始附件、AFT冻结证据和临时输出目录，复现结果写入`99_临时中转/当前四问一键复现`，最后逐字节核对当前批准核心表。
