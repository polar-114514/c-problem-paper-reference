# 第一问已有图制图数据

本目录只保留复画现有8张数据审计SVG及第一问模型关系图所需的冻结数据，不包含历史脚本、日志或废弃模型。

- `绘图基础数据.csv`及`图01`、`图03`、`图04`、`图06`、`图07`、`图08`表来自整理前已审核的数据审计包；
- `第一问_全样本抽血事件表.csv`来自当前第一至第三问权威计算输出；
- 各图的样本层级和百分数/比例口径以相邻提示词为准；
- 这些表用于复画，不改变第一问模型使用的267名孕妇、1012个有效抽血事件口径。

所有提示词均以本目录的相对路径读取数据，移动整个工作区后不应再依赖`99_临时中转`或桌面绝对路径。
