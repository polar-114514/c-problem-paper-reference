# 第一问图表状态

- 根目录的2份TXT是当前分段GEE模型的新制图提示词，尚未生成SVG；
- `01_已有数据审计图`中的8张SVG为此前已经生成的数据质量与结构审计图，原图、提示词及`02_制图数据`已成对恢复，所有提示词使用相对路径，可作为数据审计与683边界说明的候选图；
- 旧混合模型的3张SVG不属于当前论文所需材料，已移回后台，只保留在历史快照中。

后续不自动生成新图。需要重绘时按对应TXT使用MATLAB并导出SVG。
