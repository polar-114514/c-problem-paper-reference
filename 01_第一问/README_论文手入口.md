# 第一问论文手入口

本目录只保留论文写作需要的第一问模型、核心结果、制图提示词、表述边界和当前主模型核心代码；不存放逐次重采样结果、依赖目录或旧模型代码。

建议阅读顺序：

1. `01_模型说明/第一问_分段GEE模型卡.md`；
2. `02_核心结果/`中的中心化常数、参数、派生效应和Wald检验；
3. `03_图表与提示词/`中的已有SVG、图表状态和MATLAB重绘提示词；
4. `04_写作边界/第一问_可写结论与禁用表述.md`。
5. `05_核心代码/README_第一问代码说明.md`及两个权威脚本副本。

当前有效结论：使用267名孕妇、1012个有效抽血事件建立全样本分段Gaussian GEE。序号683前后关键关系存在显著异质性，但该边界只代表数据段差异，不等同于后段数据无效。

现有SVG的必要制图数据已随提示词保存在`03_图表与提示词/01_已有数据审计图/02_制图数据`。完整历史运行包和逐次复核材料仍保存在：

- `99_临时中转/优先问题整改_20260825`；
- 工作区同级目录`../C题论文后台材料/20260825_整理前完整快照/01_第一问`（仅作本机历史备份，不是当前复现依赖）。
