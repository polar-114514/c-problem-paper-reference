# 第一问关系建模代码与复现入口

第一问完整候选路线、冻结数据、主审代码、400 次孕妇簇自助结果和制图提示词，均保存在：

`第一问建模完整复现包_20260824`

该目录是从审核通过的临时建模区复制得到的自包含快照，保留原有相对目录，并纳入生成冻结数据所需的只读男胎 JSON 快照，以确保一键脚本不依赖其他临时目录。正式推荐模型为“二次 Logit 随机斜率混合效应模型”，候选 A、B、C 仅作为路线比较和稳健性证据。

## 一键复现

```powershell
& ".\01_第一问\04_代码\关系建模\第一问建模完整复现包_20260824\04_主审验收\一键复现第一问推荐主线.ps1"
```

流程会重新生成冻结数据、重跑边界稳健对照、统一模型比较、5×5 孕妇分组交叉验证、400 次孕妇簇自助及中文结果表。代码不生成图片；后续绘图只读取 `06_制图提示词` 中的 MATLAB-SVG 提示词。

复现包内部仍使用 `05_待用户审核` 等候选阶段目录名，这是为了保持脚本相对路径不变，不代表归档状态。正式论文材料以 `01_第一问/03_模型与推导/关系建模`、`05_结果数据/关系建模` 和 `07_手写正文素材/关系建模` 为准。
