统一制图规范

1. 保留此前已经生成的SVG及相邻TXT提示词；当前阶段不再生成新图片。
2. 后续优先使用MATLAB绘图。
3. 最终图片统一导出SVG格式。
4. 表头、标题、坐标轴、图例和图注统一使用中文。
5. 不得从图中夸大因果、临床唯一最优或统计显著性。
6. 置信区间、重采样区间和样本稀疏标记必须保留。
7. 极小P值不得显示为精确0。
8. 同一孕妇的重复记录不得在图注中冒充独立样本量。
9. 每幅图的提示词必须写明输入CSV、筛选规则、坐标、图层和输出文件名。
10. 历史模型SVG必须放在带“历史”标记的目录中，禁止直接作为当前模型结果图。
