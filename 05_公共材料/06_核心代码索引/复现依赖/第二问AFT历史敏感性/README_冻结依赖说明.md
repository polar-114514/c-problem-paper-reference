# 第二问AFT历史敏感性冻结依赖

本目录保存当前第一至第三问补充脚本生成AFT敏感性引用所需的四个最小文件。AFT估计的是“首次达到4%的区间删失时间”，与当前主模型的“计划日在场达标概率”目标量不同，因此只作为被降级的历史敏感性，不参加当前主路线排名。

这里保留脚本、数据断言、候选比较表和原运行清单，是为了让当前主路线复现不再依赖已迁走的旧目录。它们的安全散列值会进入当前运行清单；修改这些文件必须重新审核AFT引用链。
