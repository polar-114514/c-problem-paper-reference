# 第四问图表状态

本目录3张SVG均为当前第四问L1正则逻辑回归正式路线的已有图，已与各自MATLAB/SVG提示词放在同一目录：

1. 三条候选路线外层验证比较；
2. 入选模型阈值性能全曲线；
3. 入选模型特征稳定性与分型性能。

这些图可以作为当前论文候选图。三份提示词已改为读取`02_核心结果`中的当前文件名，特征稳定性、类型区间和路线差值证据也已补齐。论文手最终引用前仍需核对图号、中文图注和正文数值是否与核心CSV一致。
